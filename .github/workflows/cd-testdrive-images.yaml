name: CD Testdrive Unbound Docker Images

on:
  workflow_run:
    workflows:
      - "CD Build Canary Distroless Unbound Docker Image"
      - "CD Build Canary Distroless Unbound QUIC Docker Image"
      - "CD Build Distroless Unbound Docker Image"
      - "CD Build Distroless Unbound QUIC Docker Image"
    types:
      - completed

permissions: {}

jobs:
  test-image:
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' }}

    steps: 
      - name: Harden Runner
        uses: step-security/harden-runner@f808768d1510423e83855289c910610ca9b43176
        with:
          egress-policy: audit

      - name: Determine image tag
        id: tag
        run: |
          case "${{ github.event.workflow_run.name }}" in
            "CD Build Canary Distroless Unbound Docker Image")
              echo "IMAGE_TAG=madnuttah/unbound:canary" >> $GITHUB_ENV
              ;;
            "CD Build Canary Distroless Unbound QUIC Docker Image")
              echo "IMAGE_TAG=madnuttah/unbound:canary-quic" >> $GITHUB_ENV
              ;;
            "CD Build Distroless Unbound QUIC Docker Image")
              echo "IMAGE_TAG=madnuttah/unbound:latest-quic" >> $GITHUB_ENV
              ;;
            "CD Build Distroless Unbound Docker Image")
              echo "IMAGE_TAG=madnuttah/unbound:latest" >> $GITHUB_ENV
              ;;
            *)
              exit 1
              ;;
          esac

      - name: Login to Docker Hub
        uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Pull image
        run: docker pull "$IMAGE_TAG" || true

      # - name: Test Unbound config
        # run: |
          # if [[ "$IMAGE_TAG" == *"-quic" ]]; then
            # timeout 30s docker run --rm --entrypoint unbound-checkconf "$IMAGE_TAG" || true
          # else
            # timeout 30s docker run --rm --entrypoint unbound-checkconf "$IMAGE_TAG"
          # fi

      - name: DNS test
        run: |
          if [[ "$IMAGE_TAG" == *"-quic" ]]; then
            timeout 30s docker run -d --name unbound-dns -p 5335:5335/udp "$IMAGE_TAG" || true
            sleep 4
            timeout 10s docker exec unbound-dns drill cloudflare.com A || true
            docker rm -f unbound-dns || true
          else
            timeout 30s docker run -d --name unbound-dns -p 5335:5335/udp "$IMAGE_TAG"
            sleep 4
            timeout 10s docker exec unbound-dns drill cloudflare.com A
            docker rm -f unbound-dns
          fi

      # - name: DNSSEC validation test
        # run: |
          # timeout 30s docker run -d --name unbound-dnssec -p 5353:53/udp "$IMAGE_TAG"
          # sleep 4

          # if [[ "$IMAGE_TAG" == *"-quic" ]]; then
            # timeout 10s docker exec unbound-dnssec drill -D cloudflare.com A > quic_dnssec_ok.out
            # if ! grep -q "ad;" quic_dnssec_ok.out; then
              # docker rm -f unbound-dnssec
              # exit 1
            # fi
            # timeout 10s docker exec unbound-dnssec drill -D cloudflare.com DNSKEY > quic_dnskey.out
            # if ! grep -q "DNSKEY" quic_dnskey.out; then
              # docker rm -f unbound-dnssec
              # exit 1
            # fi
            # docker rm -f unbound-dnssec
            # exit 0
          # fi

          # timeout 10s docker exec unbound-dnssec drill -D dnssec-failed.org A > dnssec_fail.out
          # if ! grep -q "SERVFAIL" dnssec_fail.out; then
            # docker rm -f unbound-dnssec
            # exit 1
          # fi

          # timeout 10s docker exec unbound-dnssec drill -D cloudflare.com A > dnssec_ok.out
          # if ! grep -q "ad;" dnssec_ok.out; then
            # docker rm -f unbound-dnssec
            # exit 1
          # fi

          # docker rm -f unbound-dnssec
